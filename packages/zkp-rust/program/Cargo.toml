[package]
name = "credential-verifier-program"
version = "0.1.0"
edition = "2021"

[dependencies]
sp1-zkvm = "3.0.0"
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"
hex = "0.4"

[features]
default = []
